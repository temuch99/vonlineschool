<%= simple_form_for [:admin, @course, @lesson, @question], wrapper: :horizontal_form, html: { class: 'form-horizontal' } do |f| %>
	<%= f.input :task, class: "file" %>
	<%= f.input :correct_answer %>


	<div class="button-box float-right">
		<%= f.submit "Сохранить", class: 'btn btn-primary' %>
		<%= link_to "Назад", [:admin, @course, :lessons], class: "btn btn-danger" %>
	</div>
<% end %>