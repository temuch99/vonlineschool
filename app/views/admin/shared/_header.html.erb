<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <%= link_to "Админка", admin_root_path, class: "navbar-brand" %>
  <%= link_to "Вернуться на сайт", root_path, class: "navbar-brand" %>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <% @header.each do |item, options| %>
        <li class="nav-item <%= options[:active] ? 'active' : '' %>">
          <%= link_to options[:name], options[:path], class: "nav-link" %>
        </li>
      <% end %>
      <!-- <li class="nav-item dropdown">
        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
          Профиль
        </a>
      </li> -->
    </ul>
  </div>
</nav>